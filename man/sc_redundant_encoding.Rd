% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context-support.R
\name{sc_redundant_encoding}
\alias{sc_redundant_encoding}
\title{Allocate redundant shapes or patterns for confusable colors}
\usage{
sc_redundant_encoding(
  x,
  channel = c("shape", "pattern", "both"),
  min_cie2000 = 8,
  cvd = c("deutan", "protan", "tritan"),
  shapes = c(16L, 17L, 15L, 18L, 8L, 3L),
  patterns = c("none", "stripe", "crosshatch", "circle", "wave", "weave")
)
}
\arguments{
\item{x}{An \code{sc_color_map} or fully named color vector.}

\item{channel}{Allocate \code{"shape"}, \code{"pattern"}, or joint shape-pattern
combinations with \code{"both"}. A conflicting pair differs in at least one
enabled aesthetic.}

\item{min_cie2000}{Pairs below this worst-vision CIE2000 distance must receive
different auxiliary encodings.}

\item{cvd}{Vision simulations used with normal vision.}

\item{shapes}{Unique integer plotting shapes.}

\item{patterns}{Unique non-empty pattern names.}
}
\value{
A data frame containing label, unchanged color, encoding group,
texture group, shape, pattern, and number of confusable neighbors, with
\code{sc_encoding} settings and \code{conflicts} records as attributes.
}
\description{
Finds label pairs whose worst CIE2000 distance across normal and requested
CVD simulations is below \code{min_cie2000}, then assigns auxiliary encoding
groups with a deterministic DSATUR heuristic so every such pair differs.
Colors are returned unchanged.
Shapes are ggplot2-compatible integers. Default pattern names are compatible
with ggpattern; \code{texture_group} can also be used as a grouping variable for
other texture systems such as scatterHatch.
}
\details{
Allocation is deterministic for one full label set, but graph priorities can
change when labels are added or removed. Allocate once for the full label
universe and reuse or subset the returned table across figures. The
\code{sc_encoding} and \code{conflicts} attributes record the method, settings, pools,
and conflicting pairs; save the object as RDS if those attributes must persist,
because ordinary data-frame CSV export does not retain them.
}
\examples{
colors <- c(A = "#0072B2", B = "#0072B2", C = "#D55E00")
sc_redundant_encoding(colors, channel = "shape")
}
