% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relationship-map.R
\name{sc_relationship_map}
\alias{sc_relationship_map}
\title{Optimize a persistent color map from label relationships}
\usage{
sc_relationship_map(
  relationship,
  canonical = NULL,
  locked = character(),
  stability_budget = 0L,
  seed = 1L,
  cvd = c("deutan", "protan", "tritan"),
  background = c("light", "dark"),
  na.value = "#BDBDBD"
)
}
\arguments{
\item{relationship}{Symmetric numeric affinity matrix whose off-diagonal
entries lie in \verb{[0, 1]}, with the same unique, non-empty row and column
names. Diagonal values are ignored.}

\item{canonical}{Optional \code{sc_color_map} or fully named color vector. Its
labels must be a subset of the matrix labels.}

\item{locked}{Additional canonical labels that may never change, supplied as
names or a named logical vector. These are combined with locks already in
a canonical \code{sc_color_map}.}

\item{stability_budget}{Maximum number of non-locked canonical colors that
may change. The default preserves every canonical assignment.}

\item{seed}{Non-negative integer controlling label and candidate search order.}

\item{cvd}{Vision simulations used with normal vision. Values may come from
\code{"none"}, \code{"deutan"}, \code{"protan"}, and \code{"tritan"}.}

\item{background}{Background used for candidate generation and initialization.}

\item{na.value}{Color used for missing labels.}
}
\value{
A derived \code{sc_color_map} with locks, stability accounting, optimizer
context, effective relationship input, canonical baseline, and provenance.
}
\description{
Treats \code{relationship} as a symmetric affinity matrix: \code{1} asks related
labels to use perceptually closer (but still distinct) colors, while \code{0}
asks unrelated labels to be farther apart. The diagonal is ignored.
Off-diagonal entries must contain finite values in \verb{[0, 1]}; row and column
labels must be identical.
}
\details{
The method fits normal-vision CIE2000 distance to the affinity-derived target
\code{8 + 32 * (1 - affinity)}. A separate penalty applies when the worst distance
across normal vision and requested CVD simulations falls below that target.
Distances above 40 are treated as equivalent. This is a deterministic,
seeded heuristic and a diagnostic accessibility aid, not a guarantee of
universal CVD safety.

Canonical colors form the baseline. Hard-locked colors never change;
\code{stability_budget} is the maximum number of other canonical assignments
that may change when doing so strictly improves the objective. New labels do
not consume the budget. The normalized effective relationship input,
baseline, method settings, outcome, seed, and provenance are stored in the
returned map. Construction provenance remains immutable when a map is
subset; rerun this function to calculate metrics for a different label set.
Matrices returned by \code{\link[=sc_relationship_from_knn]{sc_relationship_from_knn()}} also carry their
coordinate-construction settings and pairwise sample support into the map.
}
\examples{
affinity <- matrix(
  c(1, .9, .1, .9, 1, .2, .1, .2, 1), nrow = 3,
  dimnames = list(c("CD4 T", "CD8 T", "B"), c("CD4 T", "CD8 T", "B"))
)
canonical <- sc_color_map(c("CD4 T", "B"))
map <- sc_relationship_map(affinity, canonical, locked = "B", seed = 42)
as_named_colors(map)
}
