% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales-discrete.R
\name{scale_color_sc_map}
\alias{scale_color_sc_map}
\alias{scale_colour_sc_map}
\alias{scale_fill_sc_map}
\title{Manual ggplot2 scales from a persistent color map}
\usage{
scale_color_sc_map(map, ..., drop = TRUE)

scale_colour_sc_map(map, ..., drop = TRUE)

scale_fill_sc_map(map, ..., drop = TRUE)
}
\arguments{
\item{map}{An \code{sc_color_map}.}

\item{...}{Passed to \code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}} or
\code{\link[ggplot2:scale_fill_manual]{ggplot2::scale_fill_manual()}}.}

\item{drop}{Drop unused factor levels from the scale.}
}
\value{
A ggplot2 scale.
}
\description{
These scales use the exact named assignments stored in \code{map}; assignments
are never recomputed from the plotted subset.
}
\details{
Use the color or colour variant for points, lines, text, and geometry
outlines mapped with \code{color}. Use the fill variant for geometry interiors
mapped with \code{fill}, such as violins, bars, tiles, and polygons. The
\verb{scale_colour_*()} spelling is an exact alias of \verb{scale_color_*()}. For a
one-off categorical plot that does not require locked assignments, use the
\verb{_sc_d()} family; for numeric gradients, use \verb{_sc_c()}.
}
\examples{
map <- sc_color_map(c("B", "T", "NK"))
ggplot2::ggplot(
  data.frame(x = 1:2, y = 1:2, cell = c("T", "NK")),
  ggplot2::aes(x, y, color = cell)
) + ggplot2::geom_point() + scale_color_sc_map(map)
}
